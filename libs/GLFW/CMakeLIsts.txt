cmake_minimum_required(VERSION 3.10)

set(CMAKE_CXX_STANDARD 11)

find_package(OpenGL REQUIRED)

add_library(GLFW STATIC glfw3.h  glfw3native.h ${CMAKE_SOURCE_DIR}/libs/GLFW/glfw3.lib)

target_include_directories(GLFW PUBLIC "./")
target_link_libraries(GLFW PUBLIC backend imgui OpenGL::GL)



